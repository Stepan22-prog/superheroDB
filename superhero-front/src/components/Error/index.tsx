import { Typography } from "@mui/material";

export default function Error() {
  return (
    <Typography variant="h4" color="error" textAlign="center">Error! Unable to load data. Reload page</Typography>
  )
}
